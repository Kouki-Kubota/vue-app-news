// initial state
const state = () => ({
    newsData: {
        "technology": "technology",
        "entertainment": "entertainment",
        "business": "business",
        "science": "science",
    },
    activeCategory: "sport",
    bookmarkData:[]
})

// getters
const getters = {}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}